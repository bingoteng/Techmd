# mqueue模块的详细设计

## 1.模块概述

- 概述
  
  ​    该消息队列模块是`Starheart RTOS`中实现对`POSIX`消息传递机制的系统层的移植适配（POSIX 1003.1-2001标准）。三层架构。顶层提供对外API;中间层预分配对象池和描述符池管理所有队列资源，使用双重引用计数，配合三级自旋锁实现细颗粒度的并发控制。底层封装内核消息队列完成实际的消息缓冲和收发。

- 使用场景
  
  ​    多用于实时数据采集和优先级处理，多线程日志管理，线程池任务分发等等场景

## 2.模块接口

### 2.1对外接口

| 接口名称                  | 功能描述          | 备注                                  |
| --------------------- | ------------- | ----------------------------------- |
| `mq_open`             | 打开或创建命名消息队列   | 支持`O_CREAT, O_EXCL, O_NONBLOCK `等标志 |
| `mq_close`            | 关闭消息队列描述符     | 减少引用计数，可能触发延迟销毁                     |
| `mq_unlink`           | 移除消息队列名称      | 标记删除，实际销毁推迟至最后描述符关闭                 |
| `mq_send`             | 向消息队列发送消息（阻塞） | mq_timedsend 的无超时封装                 |
| `mq_timedsend`        | 带超时的消息发送      | 核心发送实现，接受绝对超时时间                     |
| `mq_receive`          | 从消息队列接收消息（阻塞） | mq_timedreceive 的无超时封装              |
| `mq_timedreceive`     | 带超时的消息接收      | 核心接收实现，返回实际接收字节数                    |
| `mq_getattr`          | 获取消息队列当前属性    | 返回 flags, maxmsg, msgsize, curmsgs  |
| `mq_setattr`          | 设置消息队列属性      | 仅允许修改 O_NONBLOCK 标志                 |
| `_mqueue_module_init` | 模块初始化         | 初始化全局链表、锁及预分配对象/描述符池                |

### 2.2调用关系

```
创建打开/打开已有消息队列->发送消息->接收消息->属性操作->关闭消息队列
```

```mermaid
graph TD
    subgraph "应用程序层"
        APP[应用程序]
    end

    subgraph "POSIX标准接口（对外API）"
        API1[mq_open<br/>创建/打开队列]
        API2[mq_close<br/>关闭描述符]
        API3[mq_unlink<br/>删除队列]
        API4[mq_send<br/>发送消息]
        API5[mq_receive<br/>接收消息]
        API6[mq_timedsend<br/>带超时发送]
        API7[mq_timedreceive<br/>带超时接收]
        API8[mq_getattr<br/>获取属性]
        API9[mq_setattr<br/>设置属性]
    end

    subgraph "内部核心函数（辅助层）"
        INT1[posix_mq_validate_name<br/>名称验证]
        INT2[mq_obj_find_by_name_locked<br/>查找队列对象]
        INT3[mq_obj_alloc/mq_obj_free<br/>对象池管理]
        INT4[mq_desc_alloc/mq_desc_free<br/>描述符池管理]
        INT5[mq_obj_ref/mq_obj_deref<br/>引用计数管理]
        INT6[calculate_relative_timeout_ms<br/>超时计算]
        INT7[srh_dlist_insert_tail/remove<br/>链表操作]
    end

    APP --> API1
    APP --> API2
    APP --> API3
    APP --> API4
    APP --> API5
    APP --> API8
    APP --> API9

    API1 --> INT1
    API1 --> INT4
    API1 --> INT2
    API1 --> INT3
    API1 --> INT7

    API2 --> INT4
    API2 --> INT5
    API2 --> INT7

    API3 --> INT1
    API3 --> INT2
    API3 --> INT5
    API3 --> INT7

    API4 --> API6
    API5 --> API7

    API6 --> INT5
    API6 --> INT6

    API7 --> INT5
    API7 --> INT6

    API8 --> INT5
    API8 --> INT7

    API9 --> INT5

    style API1 fill:#e1f5fe
    style API2 fill:#e1f5fe
    style API3 fill:#e1f5fe
    style API4 fill:#f3e5f5
    style API5 fill:#f3e5f5
    style API6 fill:#f3e5f5
    style API7 fill:#f3e5f5
    style INT1 fill:#c8e6c9
    style INT2 fill:#c8e6c9
    style INT3 fill:#c8e6c9
    style INT4 fill:#c8e6c9
    style INT5 fill:#c8e6c9
    style INT6 fill:#c8e6c9
```

## 3.模块设计原理

### 1.设计原理：

​    描述符和对象分离思想的设计模式

1. 对象：`posix_mq_object_t` 系统级共享的实体（唯一）

2. 描述符：`posix_mq_desc_t`线程级（多个线程描述符可以映射同一个对象：对应多线程对同一消息队列的多次打开）

3. 静态池：
   
   1. `g_mq_list_head`：活跃对象池：串联管理有效的消息队列
   
   2. `g_mq_free_head`：空闲对象池：分配可用的消息队列
   
   3. 同一时刻`posix_mq_object_t`中的`list_node`只属于其中一个

4. 描述符池：`g_mq_desc_free_head`:管理可用描述符

5. 架构分层
   
   ```
   ┌───────────────────────────────────────────────────────┐
   │         POSIX API 层 (mq_open, mq_send...)        │  ← 标准接口，参数校验，errno设置
   ├───────────────────────────────────────────────────────┤
   │       对象管理层 (Alloc/Free/Ref/Deref)            │  ← 对象池操作，引用计数，生命周期
   ├───────────────────────────────────────────────────────┤
   │          全局资源管理层 (List/Lock/Init)            │  ← 命名空间维护，描述符表，模块初始化
   ├───────────────────────────────────────────────────────┤
   │          StarHeart RTOS 内核层 (srh_ipc_mqueue)    │  ← 实际IPC通信，阻塞/超时调度
   └───────────────────────────────────────────────────────┘
   ```

### 2.资源关系

#### 1.结构关系列表

```mermaid
erDiagram
    GlobalResources ||--o{ posix_mq_desc_t : "管理空闲池"
    GlobalResources ||--o{ posix_mq_object_t : "管理活跃/空闲链表"
    GlobalResources ||--o{ 静态资源static : "管理静态资源"

    posix_mq_desc_t }o--|| posix_mq_object_t : "mq_object指向 (多对一)"
    posix_mq_object_t ||--|| srh_ipc_mqueue_endpoint : "native_mq持有 (1:1)"
    posix_mq_object_t ||--|| mq_attr : "attributes内嵌 (1:1)"

    posix_mq_desc_t {
        s_srh_dlist_node desc_list_node "串联描述符"
        posix_mq_object_t* mq_object "指向共享对象"
        int access_mode_flags "访问权限"
    }

    posix_mq_object_t {
        s_srh_dlist_node list_node "串联对象"
        int open_count "描述符引用计数"
        int ref_count "操作进行中计数"
        bool unlinked "延迟删除标记"
        s_srh_spinlock state_lock "保护对象状态"
        srh_ipc_mqueue_endpoint* native_mq "内核端点"
        mq_attr attributes "队列属性"
    }

    静态资源static{
    s_srh_dlist_node g_mq_desc_free_head "描述符链表头"
    s_srh_dlist_node g_mq_list_head    "活跃对象链表头"
     s_srh_dlist_node g_mq_free_head    "空闲对象链表头"
     s_srh_spinlock g_mq_desc_lock    "描述符锁"
    s_srh_spinlock g_mq_list_lock    "全局活跃对象链表锁"
    s_srh_spinlock g_mq_free_lock    "空闲对象链表锁"
    }
    mq_attr {
    long mq_flags    "消息队列文件标志" 
    long mq_maxmsg    "消息队列可容纳的最大消息个数" 
    long mq_msgsize "消息队列的单则消息的最大长度"
    long mq_curmsgs "当前消息队列中的消息数量(mq_getattr填充)" 
    }

```

#### 2.结构关系框图

```mermaid
graph TB
    subgraph "全局资源层 GlobalResources"
        GR[GlobalResources]
        GR --> |g_mq_list_head| AL[活跃对象链表]
        GR --> |g_mq_free_head| FP[空闲对象池]
        GR --> |g_mq_desc_free_head| DFP[空闲描述符池]
        GR --> |锁保护| LK[desc_lock→list_lock→state_lock]
    end

    subgraph "系统对象层 posix_mq_object_t"
        OBJ[posix_mq_object_t<br/>系统级共享实体]
        OBJ --> |1:1持有| KERN[srh_ipc_mqueue_endpoint<br/>内核IPC端点]
        OBJ --> |1:1内嵌| ATTR[mq_attr<br/>POSIX属性]
        OBJ --> |双向链表| OBJ2[下一个对象]
        OBJ --> |open_count| OC[引用计数<br/>mq_open/mq_close]
        OBJ --> |ref_count| RC[操作计数<br/>mq_send/mq_receive]
        OBJ --> |unlinked| UL[延迟删除标记]
    end

    subgraph "描述符层 posix_mq_desc_t"
        DESC[posix_mq_desc_t<br/>线程级私有句柄]
        DESC --> |mq_object指向| OBJ
        DESC --> |双向链表| DESC2[下一个描述符]
        DESC --> |access_mode| FLAGS[O_RDONLY/W/RDWR]
    end

    subgraph "资源池关系"
        FP -.-> |分配| OBJ
        DFP -.-> |分配| DESC
        AL -.-> |串联| OBJ
    end

    style GR fill:#ffd93d
    style OBJ fill:#6bcb77
    style DESC fill:#4d96ff
    style KERN fill:#ff6b6b
    style ATTR fill:#a66cff
```

### 3.1`mq_open`

#### 1.API原型

```c
#include <mqueue.h>
#include <sys/stat.h>   /* For mode constants */
#include <fcntl.h>      /* For O_* constants */mqd_t 

mqt_t mq_open(const char *name, int oflag);

mqd_t mq_open(const char *name, int oflag, mode_t mode, struct mq_attr *attr);

eg:
mqd_t mqdes = mq_open(“test_mq”, O_CREAT | O_RDWR, 0x777, &attr);
```

#### 2.参数说明

| 项目         | 描述                                                                                                                                                                                                                                                |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 函数功能       | 打开或创建一个 POSIX **命名**消息队列。返回一个消息队列描述符，用于后续的发送、接收及关闭操作。若指定了创建标志且队列不存在，则根据 `mode` 和 `attr` 创建新队列；若已存在且未指定排他标志，则直接打开。                                                                                                                                 |
| 参数 `name`  | 消息队列名称字符串。不同进程通过相同名称访问同一队列。                                                                                                                                                                                                                       |
| 参数 `oflag` | 操作标志位组合：<br>• `O_RDONLY`: 只读（仅接收）<br>• `O_WRONLY`: 只写（仅发送）<br>• `O_RDWR`: 读写<br>• `O_CREAT`: 队列不存在时创建,附加两个参数：mode和attr<br/>• `O_EXCL`: 配合 `O_CREAT` 使用，若队列已存在则失败<br>• `O_NONBLOCK`: 非阻塞模式：队列为空时的读和队列为满时的写不被阻塞。                                    |
| 参数 `mode`  | 权限掩码（如 `0644`），仅在创建新队列时有效，定义队列文件的访问权限。                                                                                                                                                                                                            |
| 参数 `attr`  | 指向 `struct mq_attr` 的指针，指定队列属性（最大消息数、最大消息大小、优先级等）。传 `NULL` 时使用系统默认值。仅在创建新队列时生效。                                                                                                                                                                   |
| 返回值        | 成功返回消息队列描述符 (`mqd_t`)；失败返回 `(mqd_t)-1` 并设置 `errno`。                                                                                                                                                                                               |
| 错误码        | • `EACCES`: 权限不足或 name 格式非法<br/>• `EEXIST`: O_CREAT \| O_EXCL 下队列已存在<br/>• `ENOENT`: 未指定 O_CREAT 且队列不存在<br/>• `EINVAL`: name 无效、attr 中字段非法或超出系统限制<br/>• `EMFILE/ENFILE`: 进程或系统级描述符耗尽<br/>• `ENAMETOOLONG`: name 长度超限<br/>• `ENOMEM`: 内核内存不足无法创建队列 |
| 注意事项       | •编译命令包含 -lrt                                                                                                                                                                                                                                      |
| 使用场景       | POSIX 进程间通信（IPC）中基于消息队列的数据交换；实时系统中任务间异步消息传递；需要持久化命名队列（内核级生命周期）的场景。                                                                                                                                                                                |

#### 3.实现流程

##### 1.正常执行流图

![image-20260722145427061](assets/image-20260722145427061.png)

##### 2.详细流程图

```mermaid
flowchart TD
    subgraph Phase1 [阶段一: 参数解析]
        style Phase1 fill:#e1f5fe,stroke:#01579b,stroke-width:3px
        A[开始 mq_open] --> B{oflags 是否<br>包含 O_CREAT?}
        B -->|是| C[解析可变参数:<br>跳过 mode, 获取 attr]
        B -->|否| D[初始化默认属性]
        C --> D
    end

    D --> E

    subgraph Phase2 [阶段二: 参数校验]
        style Phase2 fill:#fff3e0,stroke:#e65100,stroke-width:3px
        E[名称验证<br>posix_mq_validate_name] --> F{名称合法?}
        F -->|否| ERR1[错误处理]

        F -->|是| H{是 O_CREAT<br>且 attr 非 NULL?}
        H -->|是| I{attr 参数合法?<br>mq_maxmsg > 0<br>mq_msgsize > 0}
        I -->|否| ERR2[错误处理]
        I -->|是| K[用用户属性<br>覆盖默认值]

        H -->|否| L{访问模式合法?<br>O_RDONLY / O_WRONLY / O_RDWR}
        K --> L
        L -->|否| ERR3[错误处理]
    end

    L -->|是| N

    subgraph Phase3 [阶段三: 资源预分配]
        style Phase3 fill:#f3e5f5,stroke:#6a1b9a,stroke-width:3px
        N[分配描述符<br>mq_desc_alloc] --> O{分配成功?}
        O -->|否| ERR4[错误处理]
    end

    O -->|是| Q

    subgraph Phase4 [阶段四: 查找或创建内核对象]
        style Phase4 fill:#e8f5e9,stroke:#1b5e20,stroke-width:3px
        Q[获取全局锁<br>srh_spinlock_lock] --> R[在全局链表中<br>查找同名队列]

        R --> S{队列已存在?}

        S -->|是| T{设置了 O_EXCL?}
        T -->|是| ERR5[错误处理: 需先释放锁]

        T -->|否| V[增加打开计数<br>open_count++<br>重置 unlinked=false]
        V --> W[释放全局锁]

        S -->|否| X{设置了 O_CREAT?}
        X -->|否| ERR6[错误处理: 需先释放锁]

        X -->|是| Z[分配队列对象<br>mq_obj_alloc]
        Z --> AA{分配成功?}
        AA -->|否| ERR7[错误处理: 需先释放锁]

        AA -->|是| AC[初始化队列对象:<br>open_count=1, ref_count=1<br>复制名称和属性<br>状态=ST_UND]
        AC --> AD[插入全局链表尾部]
        AD --> AE[释放全局锁]
        AE --> AF[创建底层原生队列<br>srh_ipc_mqueue_endpoint_create]
        AF -----> AG{创建成功?}
        AG -->|否| ERR8[错误处理: 缺少从链表移除]
        AG -->|是| AI[设置状态<br>mq_st = ST_INITED]
        AI --> W
    end

    W --> AJ

    subgraph Phase5 [阶段五: 并发同步等待]
        style Phase5 fill:#fce4ec,stroke:#880e4f,stroke-width:3px
        AJ[等待初始化完成<br>while mq_st < ST_INITED] --> AK
    end

    AK --> AL

    subgraph Phase6 [阶段六: 完成返回]
        style Phase6 fill:#e0f2f1,stroke:#004d40,stroke-width:3px
        AK[关联描述符与对象<br>设置访问模式] --> AL[返回描述符 mqd_t]
    end

    subgraph ErrorHandler [错误处理集中区]
        style ErrorHandler fill:#ffebee,stroke:#b71c1c,stroke-width:4px
        ERR1[名称非法<br>errno 已设置] --> ERR_END[返回 -1]
        ERR2[属性非法<br>errno = EINVAL] --> ERR_END
        ERR3[访问模式非法<br>errno = EINVAL] --> ERR_END
        ERR4[描述符分配失败<br>errno = ENFILE] --> ERR_END
        ERR5[O_EXCL 冲突<br>释放锁 + 释放描述符<br>errno = EEXIST] --> ERR_END
        ERR6[队列不存在且无 O_CREAT<br>释放锁 + 释放描述符<br>errno = ENOENT] --> ERR_END
        ERR7[对象分配失败<br>释放锁 + 释放描述符<br>errno = ENFILE] --> ERR_END
        ERR8[底层队列创建失败<br>释放描述符 + 释放对象<br>errno = ENOSPC<br>⚠️ 缺少从链表移除] --> ERR_END
    end
```

### 3.2`mq_close`

#### 1.API原型

```c
#include <mqueue.h>

int mq_close(mqd_t mqdes);
```

#### 2.参数说明

| 项目         | 描述                                                                            |
| ---------- | ----------------------------------------------------------------------------- |
| 函数功能       | 关闭一个 POSIX 消息队列描述符，释放该描述符占用的进程资源。**注意：它不会删除消息队列本身，队列及其中的未读消息在内核中继续存在。**       |
| 参数 `mqdes` | 由 `mq_open()` 成功返回的消息队列描述符。                                                   |
| 返回值        | 成功返回 `0`；失败返回 `-1` 并设置 `errno`。                                               |
| 错误码        | `EBADF`：无效描述符                                                                 |
| 注意事项       | •关闭 ≠ 删除，`mq_close` 仅解除当前进程与该队列的关联，消息队列仍驻留内核。                                 |
| 使用场景       | 进程完成对某消息队列的读写操作后释放描述符；多进程协作中某个进程退出前清理自身资源；配合 `mq_unlink()` 实现"先关闭再删除"的安全销毁流程。 |

#### 3.实现流程

##### 1.正常执行流图

![image-20260722151707614](assets/image-20260722151707614.png)

##### 2.详细流程图

```mermaid
flowchart TD
    subgraph Phase1 [阶段一: 参数校验与描述符查找]
        style Phase1 fill:#e1f5fe,stroke:#01579b,stroke-width:3px
        A[开始 mq_close] --> B{描述符 mqdes<br>是否有效?}
        B -->|否| ERR1[错误处理]
        B -->|是| C[将 mqdes 转换为<br>posix_mq_desc_t 指针]
        C --> D[从描述符中<br>获取关联的 mq_object]
        D --> E{mq_object<br>是否有效?}
        E -->|否| ERR2[错误处理]
    end

    E -->|是| F

    subgraph Phase2 [阶段二: 获取对象锁并标记删除]
        style Phase2 fill:#fff3e0,stroke:#e65100,stroke-width:3px
        F[获取对象状态锁<br>srh_spinlock_lock] --> G[递减打开计数<br>mq_object->open_count--]
        G --> H{open_count<br>是否降为 0?}
        H -->|否| I[仅释放状态锁<br>srh_spinlock_unlock]
        H -->|是| J{对象是否被标记<br>为 unlinked?}
        J -->|否| K[释放状态锁<br>srh_spinlock_unlock]
        J -->|是| L[释放状态锁<br>srh_spinlock_unlock]
    end

    I --> M
    K --> M
    L --> N

    subgraph Phase3 [阶段三: 释放描述符资源]
        style Phase3 fill:#f3e5f5,stroke:#6a1b9a,stroke-width:3px
        M[将描述符的 mq_object<br>指针置空] --> O[释放描述符<br>mq_desc_free]
    end

    O --> END2[返回 0]

    subgraph Phase4 [阶段四: 必要时销毁内核对象]
        style Phase4 fill:#e8f5e9,stroke:#1b5e20,stroke-width:3px
        N{open_count == 0<br>且 unlinked == true?} ----->|是| Q[获取全局列表锁<br>srh_spinlock_lock]
        Q --> R[从全局链表中<br>移除 mq_object]
        R --> S[释放全局列表锁<br>srh_spinlock_unlock]
        S --> T[销毁底层原生队列<br>srh_ipc_mqueue_endpoint_delete]
        T --> U[递减引用计数<br>ref_count--]
        U --> V{ref_count == 0?}
        V -->|是| W[释放内核对象内存<br>mq_obj_free]
        V -->|否| END1[返回 0]
        W --> END1

        N -->|否| END1
    end



    subgraph ErrorHandler [错误处理集中区]
        style ErrorHandler fill:#ffebee,stroke:#b71c1c,stroke-width:4px
        ERR1[描述符无效<br>errno = EBADF] --> ERR_END[返回 -1]
        ERR2[mq_object 无效<br>errno = EBADF] --> ERR_END
    end
```

### 3.3`mq_unlink`

#### 1. API原型

```c
#include <mqueue.h>

int mq_unlink(const char *name);
```

#### 2.参数说明

| 项目        | 描述                                                                                                                                                                            |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 函数功能      | 删除指定名称的 POSIX 消息队列。该操作仅移除队列的命名引用（类似文件系统的 unlink），不会立即销毁队列。当所有已打开该队列的描述符都被关闭后，内核才真正回收队列资源及其中未读的消息。                                                                             |
| 参数 `name` | 要删除的消息队列名称。格式要求与 `mq_open` 一致                                                                                                                                                 |
| 返回值       | 成功返回 `0`；失败返回 `-1` 并设置 `errno`。                                                                                                                                               |
| 错误码       | • `EINVAL`:无效指针<br />• `ENOENT`:队列不存在<br />• `ENAMETOOLONG`：name参数过长                                                                                                          |
| 注意事项      | • unlink ≠ 立即销毁<br />• 不影响已打开的描述符<br />•与`mq_close`的销毁条件相同，执行顺序                                                                                                               |
| 使用场景      | 消息队列生命周期结束时彻底销毁队列；程序异常退出后的清理逻辑；防止重启时因残留队列导致 `O_CREAT                                                                                                                          |
| 核心语义      | mq_unlink 做的事：<br/>  1. 设置 unlinked = true（标记为“待删除”）<br/>  2. 阻止新的 mq_open 找到这个队列<br/>  3. 如果当前无人使用（open_count == 0），立即销毁<br/>  4. 如果还有人使用（open_count > 0），等最后一个 mq_close 来销毁 |

#### 3.处理流程

##### 1.正常执行流图

![image-20260722154305307](assets/image-20260722154305307.png)

##### 2.详细流程图

```mermaid
flowchart TD
    subgraph Phase1 [阶段一: 参数校验]
        style Phase1 fill:#e1f5fe,stroke:#01579b,stroke-width:3px
        A[开始 mq_unlink] --> B[名称验证<br>posix_mq_validate_name]
        B --> C{名称合法?}
        C -->|否| ERR1[错误处理]
    end

    C -->|是| D

    subgraph Phase2 [阶段二: 持锁查找对象]
        style Phase2 fill:#fff3e0,stroke:#e65100,stroke-width:3px
        D[获取全局队列链表锁<br>srh_spinlock_lock] --> E[在全局链表中<br>查找同名队列<br>mq_obj_find_by_name_locked]
        E --> F{对象是否找到<br>且 unlinked == false?}
        F -->|否: 未找到或已被unlink| G[释放全局锁<br>srh_spinlock_unlock]
        G --> ERR2[错误处理]
    end

    F -->|是: 找到有效对象| H

    subgraph Phase3 [阶段三: 标记unlinked并判断销毁]
        style Phase3 fill:#e8f5e9,stroke:#1b5e20,stroke-width:3px
        H[设置 unlinked = true] --> I{open_count == 0<br>且 unlinked == true?}
        I -->|是| J[从全局链表中移除<br>srh_dlist_remove]
        J --> K[标记 found_and_removed = true]
        I -->|否| L[found_and_removed 保持 false]
        K --> M[释放全局锁<br>srh_spinlock_unlock]
        L --> M
    end

    M --> N

    subgraph Phase4 [阶段四: 条件销毁内核对象]
        style Phase4 fill:#fce4ec,stroke:#880e4f,stroke-width:3px
        N{found_and_removed<br>== true?}
        N -->|是| O[销毁底层原生队列<br>srh_ipc_mqueue_endpoint_destroy]
        O --> P[递减引用计数<br>mq_obj_deref]
        P --> Q[返回 0]
        N -->|否| Q
    end

    subgraph ErrorHandler [错误处理集中区]
        style ErrorHandler fill:#ffebee,stroke:#b71c1c,stroke-width:4px
        ERR1[名称非法<br>errno 由校验函数设置] --> ERR_END[返回 -1]
        ERR2[对象不存在或已unlink<br>errno = ENOENT] --> ERR_END
    end
```

### 3.4`mq_timedsend`

#### 1.API原型

```c
#include <mqueue.h>
#include <time.h>

int mq_timedsend(mqd_t mqdes,
                 const char *msg_ptr,
                 size_t msg_len,
                 unsigned int msg_prio,
                 const struct timespec *abstime);
```

#### 2.参数说明

| 项目            | 描述                                                                                                                                                                                                         |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 函数功能          | 向 POSIX 消息队列发送一条带优先级的消息。当队列满时，阻塞等待直到有空间或到达指定的绝对超时时间。相比 `mq_send`，它提供了精确的超时控制能力，避免无限期阻塞。                                                                                                                    |
| 参数 `mqdes`    | 由 `mq_open()` 返回的消息队列描述符，必须以**写**或**读写**模式打开。                                                                                                                                                              |
| 参数 `msg_ptr`  | 指向待发送消息数据的缓冲区指针。数据会被完整拷贝到内核队列中。                                                                                                                                                                            |
| 参数 `msg_len`  | 消息字节长度。必须 ≤ 创建队列时指定的 `mq_msgsize`，否则调用失败。                                                                                                                                                                  |
| 参数 `msg_prio` | 消息优先级（0 ~ 31）。数值越大优先级越高；相同优先级按 FIFO 排序。接收端总是先获取最高优先级的消息。                                                                                                                                                   |
| 参数 `abstime`  | 指向 `struct timespec` 的指针，指定绝对截止时间（CLOCK_REALTIME）。若当前时间已超过该值且队列仍满，则立即返回超时错误。传 `NULL` 等价于无限阻塞（行为同 `mq_send`）。                                                                                               |
| 返回值           | 成功返回 `0`；失败返回 `-1` 并设置 `errno`。注意：返回 0 仅表示消息已入队，不代表已被接收方读取。                                                                                                                                                |
| 错误码           | • `EAGAIN`: 队列已满且设置了 O_NONBLOCK<br/>• `EBADF`: 描述符无效或未以写模式打开<br/>• `EINTR`: 等待期间被信号中断<br/>• `EINVAL`: msg_len 超限、优先级非法、abstime 无效或指针为空<br/>• `ETIMEDOUT`: 在截止时间前仍无法入队（超时）<br/>• `EMSGSIZE`: 消息长度超过系统允许的最大值 |
| 注意事项          |                                                                                                                                                                                                            |
| 使用场景          | 实时系统中需要严格时限保证的消息发送；生产者-消费者模型中防止发送端因队列满而死锁；需要区分消息紧急程度的调度场景。                                                                                                                                                 |

#### 3.实现流程

##### 1.正常执行流图

![image-20260723095725980](assets/image-20260723095725980.png)

##### 2.详细实现流程图

```mermaid
flowchart TD
    subgraph Phase1 [阶段一: 参数校验与对象查找]
        style Phase1 fill:#e1f5fe,stroke:#01579b,stroke-width:3px
        A[开始 mq_timedsend] --> B{msg_ptr<br>是否为 NULL?}
        B -->|是| ERR1[错误处理:<br>errno = EINVAL]
        B -->|否| C[获取描述符链表锁<br>srh_spinlock_lock]
        C --> D[查找描述符<br>mq_desc_find_locked]
        D --> E[获取关联的<br>shared_mq_object]
        E --> F{shared_mq_object<br>是否有效?}
        F -->|否| G[释放描述符锁<br>srh_spinlock_unlock]
        G --> ERR2[错误处理:<br>errno = EBADF]
        F -->|是| H[增加对象引用计数<br>mq_obj_ref]
        H --> I[释放描述符锁<br>srh_spinlock_unlock]
    end

    I --> J

    subgraph Phase2 [阶段二: 权限与参数校验]
        style Phase2 fill:#fff3e0,stroke:#e65100,stroke-width:3px
        J{访问模式允许写入?<br>O_WRONLY 或 O_RDWR}
        J -->|否| ERR3[错误处理:<br>errno = EBADF<br>需先 mq_obj_deref]
        J -->|是| K{消息优先级<br>msg_prio ≤ MQ_PRIO_MAX?}
        K -->|否| ERR4[错误处理:<br>errno = EINVAL<br>需先 mq_obj_deref]
        K -->|是| L[读取队列属性<br>queue_obj_flags]
        L --> M{消息长度<br>msg_len ≤ mq_msgsize?}
        M -->|否| ERR5[错误处理:<br>errno = EMSGSIZE<br>需先 mq_obj_deref]
    end

    M -->|是| N

    subgraph Phase3 [阶段三: 超时时间计算]
        style Phase3 fill:#f3e5f5,stroke:#6a1b9a,stroke-width:3px
        N[调用 calculate_relative_timeout_ms<br>根据 abstime 和 O_NONBLOCK 标志<br>计算内核超时值 srh_timeout_ms]
        N --> O{计算成功?}
        O -->|否| ERR6[错误处理:<br>errno 由辅助函数设置<br>需先 mq_obj_deref]
    end

    O -->|是| P

    subgraph Phase4 [阶段四: 调用内核发送]
        style Phase4 fill:#e8f5e9,stroke:#1b5e20,stroke-width:3px
        P[调用内核API<br>ipc_mqueue_send<br>传入: native_mq, msg_ptr,<br>msg_len, msg_prio, timeout]
        P --> Q[递减引用计数<br>mq_obj_deref]
    end

    Q --> R

    subgraph Phase5 [阶段五: 结果处理]
        style Phase5 fill:#e0f2f1,stroke:#004d40,stroke-width:3px
        R{内核返回状态<br>srh_status}
        R -->|SRH_OK| S[返回 0<br>发送成功]
        R -->|SRH_E_PARAM| T[errno = EINVAL]
        R -->|SRH_EPERM| U[errno = EPERM]
        R -->|SRH_EAGAIN| V[errno = EAGAIN]
        R -->|SRH_ETIMEDOUT| W[errno = ETIMEDOUT]
        R -->|其他错误| X[errno = EIO]
        T --> Y[返回 -1]
        U --> Y
        V --> Y
        W --> Y
        X --> Y
    end

    subgraph ErrorHandler [错误处理集中区]
        style ErrorHandler fill:#ffebee,stroke:#b71c1c,stroke-width:4px
        ERR1 --> ERR_END[返回 -1]
        ERR2 --> ERR_END
        ERR3 --> ERR3_CLEAN[调用 mq_obj_deref] --> ERR_END
        ERR4 --> ERR4_CLEAN[调用 mq_obj_deref] --> ERR_END
        ERR5 --> ERR5_CLEAN[调用 mq_obj_deref] --> ERR_END
        ERR6 --> ERR6_CLEAN[调用 mq_obj_deref] --> ERR_END
    end
```

### 3.5`mq_timedrecive`

#### 1.API原型

```c
#include <mqueue.h>
#include <time.h>

ssize_t mq_timedreceive(mqd_t mqdes,
                        char *msg_ptr,
                        size_t msg_len,
                        unsigned int *msg_prio,
                        const struct timespec *abstime);
```

#### 2.参数说明

| 项目            | 描述                                                                                                                                                                                                                                                          |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 函数功能          | 从 POSIX 消息队列中接收一条消息。当队列为空时，阻塞等待直到有新消息到达或到达指定的绝对超时时间。返回实际接收到的消息字节数。相比 `mq_receive`，提供了精确的超时控制能力。                                                                                                                                                             |
| 参数 `mqdes`    | 由 `mq_open()` 返回的消息队列描述符，必须以读或读写模式打开。                                                                                                                                                                                                                       |
| 参数 `msg_ptr`  | 接收消息数据的缓冲区指针。消息内容会从内核拷贝到该缓冲区中。                                                                                                                                                                                                                              |
| 参数 `msg_len`  | 接收缓冲区大小（字节）。必须 ≥ 创建队列时指定的 `mq_msgsize`，否则调用失败（与 `mq_timedsend` 的 `msg_len` 含义不同，这里是缓冲区容量而非实际数据长度）。                                                                                                                                                          |
| 参数 `msg_prio` | 可选参数。若非 `NULL`，内核会将接收到的消息的优先级值写入该指针指向的变量。若不需要优先级信息，可传 `NULL`。                                                                                                                                                                                               |
| 参数 `abstime`  | 指向 `struct timespec` 的指针，指定绝对截止时间（CLOCK_REALTIME）。若当前时间已超过该值且队列仍为空，则立即返回超时错误。传 `NULL` 等价于无限阻塞（行为同 `mq_receive`）。                                                                                                                                            |
| 返回值           | 成功返回实际接收的消息字节数（`ssize_t`，非负值）；失败返回 `-1` 并设置 `errno`。注意：返回值可能小于 `msg_len`，表示消息实际长度。                                                                                                                                                                          |
| 错误码           | • `EAGAIN`：队列空且 `O_NONBLOCK` 模式，`abstime` 被忽略；<br />• `EBADF`：`mqdes` 无效或未以读权限打开；<br />• `EINTR`：阻塞期间被信号中断，需重试；<br />• `EINVAL`：`abstime` 中 `tv_nsec` < 0 或 ≥ 1e9；`EMSGSIZE`：`msg_len` < 队列的 `mq_msgsize`，缓冲区过小；<br />• `ETIMEDOUT`：到达 `abstime` 时队列仍无可用消息。 |
| 使用场景          | 实时系统中需要严格时限保证的消息接收；消费者端防止因队列空而无限期阻塞；需要获取消息优先级以做差异化处理的调度逻辑。                                                                                                                                                                                                  |

#### 3.实现流程

##### 1.正常执行流图

<img src="assets/image-20260723095557650.png" alt="image-20260723095557650"  />

##### 2.详细实现流程图

```mermaid
flowchart TD
    subgraph Phase1 [阶段一: 参数校验与对象查找]
        style Phase1 fill:#e1f5fe,stroke:#01579b,stroke-width:3px
        A[开始 mq_timedreceive] --> B{msg_ptr<br>是否为 NULL?}
        B -->|是| ERR1[错误处理:<br>errno = EINVAL]
        B -->|否| C[获取描述符链表锁<br>srh_spinlock_lock]
        C --> D[查找描述符<br>mq_desc_find_locked]
        D --> E[获取关联的<br>shared_mq_object]
        E --> F{shared_mq_object<br>是否有效?}
        F -->|否| G[释放描述符锁<br>srh_spinlock_unlock]
        G --> ERR2[错误处理:<br>errno = EBADF]
        F -->|是| H[增加对象引用计数<br>mq_obj_ref]
        H --> I[释放描述符锁<br>srh_spinlock_unlock]
    end

    I --> J

    subgraph Phase2 [阶段二: 权限与参数校验]
        style Phase2 fill:#fff3e0,stroke:#e65100,stroke-width:3px
        J{访问模式允许读取?<br>O_RDONLY 或 O_RDWR}
        J -->|否| ERR3[错误处理:<br>errno = EBADF<br>需先 mq_obj_deref]
        J -->|是| K[读取队列属性<br>queue_obj_flags]
        K --> L{接收缓冲区大小<br>msg_len ≥ mq_msgsize?}
        L -->|否| ERR4[错误处理:<br>errno = EMSGSIZE<br>需先 mq_obj_deref]
    end

    L -->|是| M

    subgraph Phase3 [阶段三: 超时时间计算]
        style Phase3 fill:#f3e5f5,stroke:#6a1b9a,stroke-width:3px
        M[调用 calculate_relative_timeout_ms<br>根据 abstime 和 O_NONBLOCK 标志<br>计算内核超时值 srh_timeout_ms]
        M --> N{计算成功?}
        N -->|否| ERR5[错误处理:<br>errno 由辅助函数设置<br>需先 mq_obj_deref]
    end

    N -->|是| O

    subgraph Phase4 [阶段四: 调用内核接收]
        style Phase4 fill:#e8f5e9,stroke:#1b5e20,stroke-width:3px
        O[调用内核API<br>ipc_mqueue_recv<br>传入: native_mq, msg_ptr,<br>&msg_len, msg_prio, timeout]
        O --> P[递减引用计数<br>mq_obj_deref]
    end

    P --> Q

    subgraph Phase5 [阶段五: 结果处理]
        style Phase5 fill:#e0f2f1,stroke:#004d40,stroke-width:3px
        Q{内核返回状态<br>srh_status}
        Q -->|SRH_OK| R[返回 msg_len<br>接收成功]
        Q -->|SRH_E_PARAM| S[errno = EINVAL]
        Q -->|SRH_EPERM| T[errno = EPERM]
        Q -->|SRH_EAGAIN| U[errno = EAGAIN]
        Q -->|SRH_ETIMEDOUT| V[errno = ETIMEDOUT]
        Q -->|其他错误| W[errno = EIO]
        S --> X[返回 -1]
        T --> X
        U --> X
        V --> X
        W --> X
    end

    subgraph ErrorHandler [错误处理集中区]
        style ErrorHandler fill:#ffebee,stroke:#b71c1c,stroke-width:4px
        ERR1 --> ERR_END[返回 -1]
        ERR2 --> ERR_END
        ERR3 --> ERR3_CLEAN[调用 mq_obj_deref] --> ERR_END
        ERR4 --> ERR4_CLEAN[调用 mq_obj_deref] --> ERR_END
        ERR5 --> ERR5_CLEAN[调用 mq_obj_deref] --> ERR_END
    end
```


